/** Рекламная карточка. */
export interface Promo {
    /** Идентификатор. */
    id: number;

    /** Наименование иконки. */
    iconName: string;

    /** Заголовок. */
    title: string;

    /** Текст. */
    text: string;
}
